import { HTMLProps, CSSProperties } from 'react';

// eslint-disable-next-line @typescript-eslint/no-unused-vars
interface ButtonEditableProps extends HTMLProps<HTMLButtonElement> {
  title: string;
  style: CSSProperties;
}